## Project
name: tasty_bytes
version: "1.0"
config-version: 2
profile: tasty_bytes

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Variables
vars:
  SNOWFLAKE_ROLE: CR_DATA_ENGINEER
  SNOWFLAKE_DB: DEV_ACCESS
  SNOWFLAKE_SCHEMA: REPORTING
  SNOWFLAKE_WH: DEV_ADHOC_WH
  
## Models
models:
  tasty_bytes:
    staging:
      +materialized: view
    marts:
      +materialized: table
