tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "not needed"
      user: "not needed"
      role: REPLACE_ROLE
      database: REPLACE_DB
      schema: REPLACE_SCHEMA
      warehouse: REPLACE_WH
      threads: 1

    prod:
      type: snowflake
      account: "not needed"
      user: "not needed"
      role: CR_DATA_ENGINEER
      database: DEV_ACCESS
      schema: REPORTING
      warehouse: DEV_ADHOC_WH
      threads: 1
